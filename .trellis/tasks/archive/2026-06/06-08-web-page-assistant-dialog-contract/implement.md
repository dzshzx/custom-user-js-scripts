# Implement

1. Add a marked dialog contract factory to `src/web-page-assistant.user.js`.
2. Replace hard-coded selector use in dialog read/write code with contract role selectors where practical.
3. Move dialog view-model logic through the contract.
4. Add `test/web-page-assistant-dialog-contract.test.mjs`.
5. Run the targeted dialog contract test.
6. Run `npm run lint`.
