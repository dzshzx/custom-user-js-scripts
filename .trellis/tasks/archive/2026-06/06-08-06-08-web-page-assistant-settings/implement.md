# Implement

1. Add the settings contract library for testable pure behavior.
2. Refactor `src/web-page-assistant.user.js` to use the same settings interface internally.
3. Add Node tests for normalization, scope resolution, and mutation.
4. Run `npm test`.
5. Run `npm run lint`.
6. Inspect diff and commit the child batch.
