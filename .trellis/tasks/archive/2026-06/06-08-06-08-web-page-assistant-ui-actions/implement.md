# Implement

1. Add dialog and widget state builder helpers.
2. Update render functions to consume those helpers.
3. Replace root click action chain with an action handler table.
4. Add or update tests if the helper seam is available outside the userscript.
5. Run `npm test`.
6. Run `npm run lint`.
7. Inspect diff and commit the child batch.
