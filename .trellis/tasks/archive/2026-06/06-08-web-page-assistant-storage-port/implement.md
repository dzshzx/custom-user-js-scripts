# Implement

1. Add a marked `createWebPageAssistantStoragePort` factory to `src/web-page-assistant.user.js`.
2. Replace `readStoredSettings`, `writeStoredSettings`, widget position storage, and menu registration with storage port calls.
3. Add `test/web-page-assistant-storage-port.test.mjs` with fake adapters.
4. Run the targeted storage port test.
5. Run `npm run lint`.
