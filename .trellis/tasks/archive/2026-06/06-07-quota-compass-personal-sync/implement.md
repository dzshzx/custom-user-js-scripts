# Implementation Plan

1. Create and plan all child tasks.
2. Start and implement `Snapshot Archive Sync`.
3. Start and implement `Quota Panel Readability Model`.
4. Start and implement `Floating Panel Shell`.
5. Bump the Codex Quota Compass userscript version.
6. Run `npm test` and `npm run lint`.
7. Review the final diff for hidden fallback, duplicate logic, and unrelated changes.
8. Archive completed tasks, commit, and push the branch.
